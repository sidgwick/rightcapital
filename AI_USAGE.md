# API 通知系统设计文档

## 一、需求理解

### 1.1 核心问题
企业内部多个业务系统需要在关键事件发生时调用外部系统（广告系统、CRM、库存系统等）的 HTTP(S) API 进行通知。

**关键点**：
- 业务系统不关心外部 API 返回值
- 业务系统只需要确保通知请求能够稳定、可靠地送达
- 不同供应商的 API 地址、Header/Body 格式不同

### 1.2 系统目标
设计一个 "API 通知系统"：
- 对内提供统一、标准化的 HTTP API
- 各业务系统只需调用该 API，传入目标地址与请求内容
- 系统负责将通知尽可能可靠地发送到外部系统

## 二、整体架构设计

### 2.1 架构概览

```
┌─────────────┐
│ 业务系统 A  │
└──────┬──────┘
       │ HTTP POST /notify
       │ { url, headers, body }
       ▼
┌─────────────────────────────────┐
│   API 通知系统                  │
│  ┌─────────────────────────┐   │
│  │  HTTP API 层            │   │
│  │  - 参数验证             │   │
│  │  - 任务入队             │   │
│  └──────────┬──────────────┘   │
│             ▼                  │
│  ┌─────────────────────────┐   │
│  │  任务队列 (Redis)       │   │
│  │  - pending_queue        │   │
│  │  - retry_queue          │   │
│  │  - dead_letter_queue    │   │
│  └──────────┬──────────────┘   │
│             ▼                  │
│  ┌─────────────────────────┐   │
│  │  Worker 消费者          │   │
│  │  - 发起 HTTP 请求       │   │
│  │  - 处理响应/失败        │   │
│  │  - 重试调度             │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
       │ HTTP Request
       ▼
┌─────────────┐
│ 外部系统    │
│ (广告/CRM)  │
└─────────────┘
```

### 2.2 数据流

1. **接收请求**：业务系统调用内部统一 API
2. **参数验证**：验证 URL、Headers、Body 格式
3. **任务入队**：将通知任务持久化到队列
4. **异步消费**：Worker 从队列取出任务
5. **发送请求**：调用外部系统 API
6. **结果处理**：成功则删除任务，失败则重试或进入死信队列

## 三、核心技术选型

### 3.1 技术栈

| 组件 | 技术选型 | 理由 |
|------|----------|------|
| 语言 | Python | 开发效率高，异步支持好 |
| Web 框架 | FastAPI | 类型安全、自动文档、高性能 |
| 数据存储 | Redis | 内存高性能，支持多种数据结构 |
| 异步框架 | asyncio + Celery | 轻量级，适合 MVP |
| HTTP 客户端 | httpx | 支持异步，连接池 |

**不使用消息队列（RabbitMQ/Kafka）的理由**：
- 增加运维复杂度
- MVP 阶段用 Redis 的 List 结构足够
- Redis 本身已经是系统依赖（持久化），避免引入额外组件

**替代方案**：如果不用 Redis，可以用简单的内存队列（Python Queue）+ 本地文件持久化，但可靠性较低。

### 3.2 数据模型

```python
# 通知任务模型
class NotificationTask:
    id: str  # UUID
    url: str  # 目标 URL
    headers: dict  # HTTP Headers
    body: dict | str  # 请求体
    created_at: datetime  # 创建时间
    retry_count: int  # 已重试次数
    max_retries: int = 5  # 最大重试次数
    next_retry_at: datetime | None  # 下次重试时间
    status: str  # pending/success/failed
```

## 四、关键工程设计

### 4.1 投递语义：至少一次（At-Least-Once）

**选择原因**：
1. 业务场景要求"尽可能可靠送达"
2. 重复通知影响小于丢失通知
3. 实现相对简单（外部系统需幂等）

**实现方式**：
- 任务持久化到 Redis
- 发送成功后才删除任务
- 失败则重试
- 允许重复，依赖外部系统幂等性

### 4.2 可靠性与失败处理

#### 4.2.1 重试策略

- **最大重试次数**：5 次
- **退避策略**：指数退避
  - 第 1 次重试：延迟 1s
  - 第 2 次重试：延迟 2s
  - 第 3 次重试：延迟 4s
  - 第 4 次重试：延迟 8s
  - 第 5 次重试：延迟 16s
- **超时时间**：每个请求 10s

#### 4.2.2 失败分类处理

| 失败类型 | 处理策略 |
|----------|----------|
| 连接超时 | 重试 |
| 5xx 错误 | 重试 |
| 429 (Too Many Requests) | 指数退避重试 |
| 4xx 客户端错误 | 直接失败，进入死信队列 |
| 网络错误 | 重试 |

#### 4.2.3 死信队列

- 重试次数超过最大限制后，任务进入死信队列
- 死信队列永久保存，供人工干预
- 提供 API 查询和重新投递接口

### 4.3 并发控制

- Worker 进程数：可配置（默认 4）
- 每个 Worker 异步处理多个任务
- 限制单目标 URL 的并发请求数（避免压垮外部系统）

## 五、系统边界明确

### 5.1 本系统解决的问题

✅ **包含功能**：
1. 统一接收通知请求
2. 请求参数验证（URL 格式、HTTP Headers 合法性）
3. 异步任务队列
4. 可靠性投递（重试机制）
5. 失败处理（死信队列）
6. 任务状态查询接口
7. 基础日志记录（任务创建、成功、失败）

### 5.2 本系统明确不解决的问题

❌ **不包含功能**（及原因）：

1. **认证授权**
   - 原因：假设内网调用，无需复杂认证；第一版 MVP 先跳过

2. **请求内容转换/适配**
   - 原因：业务系统已经知道外部 API 格式，直接透传；转换逻辑会导致系统复杂度激增

3. **Webhook 签名验证**
   - 原因：外部系统是否需要签名由供应商决定，本系统是调用方，不是接收方

4. **详细的监控告警**
   - 原因：第一版先有基础日志，监控告警可以后续接入 Prometheus/Grafana

5. **限流保护**
   - 原因：假设内网流量可控；可以通过 Worker 数量控制并发

6. **请求优先级**
   - 原因：所有通知同等重要；优先级会增加队列复杂度

7. **事务保证**
   - 原因：通知是异步的，不要求与业务事务强一致

8. **批量通知**
   - 原因：按个通知，批量逻辑可以后续优化

**不解决这些问题的依据**：YAGNI 原则（You Aren't Gonna Need It），第一版聚焦核心功能，避免过度设计。

## 六、演进路线

### 6.1 当前 MVP (v0.1)

- 单节点部署
- Redis 持久化
- 至少一次投递
- 基础重试机制

### 6.2 未来演进方向

#### 场景 1：流量显著增长

**演进方案**：
- 部署多个 Worker 节点（横向扩展）
- 引入 Redis Cluster 或迁移到 RabbitMQ/Kafka（更好的分区和负载均衡）
- 添加连接池管理和请求速率限制

#### 场景 2：复杂度增加

**演进方案**：
- 添加 API 网关（鉴权、限流、路由）
- 接入监控系统（Prometheus + Grafana）
- 告警系统（钉钉/企业微信/邮件）
- 管理后台（任务可视化、手动重试）

#### 场景 3：需要更高的可靠性

**演进方案**：
- 引入分布式锁（避免重复投递）
- 事务消息机制（确保任务持久化）
- 多活部署（灾备）

#### 场景 4：需要更精细的投递控制

**演进方案**：
- 优先级队列
- 延迟队列（定时通知）
- 按业务线隔离队列

## 七、AI 使用说明

### 7.1 AI 提供帮助的关键地方

1. **需求分析与架构设计**：AI 帮助梳理需求边界、提出架构方案
2. **技术选型建议**：AI 对比不同技术栈的优劣
3. **代码框架生成**：AI 生成 FastAPI、Redis、Worker 的基础代码
4. **重试策略设计**：AI 建议指数退避、超时时间等参数
5. **失败分类处理**：AI 分析不同 HTTP 状态码的处理逻辑

### 7.2 AI 建议但未采纳的设计

| AI 建议 | 未采纳原因 |
|---------|-----------|
| 使用 RabbitMQ 作为消息队列 | 第一版增加运维复杂度，用 Redis 足够 |
| 实现精确一次投递 | 实现复杂，业务可以容忍重复 |
| 添加 JWT 认证 | MVP 阶段内网调用，无需认证 |
| 引入 PostgreSQL 存储任务历史 | Redis 足够持久化，简化技术栈 |
| 实现请求内容适配层 | 业务系统知道外部 API 格式，透传即可 |
| 添加分布式追踪（Jaeger） | 第一版基础日志足够 |
| 使用 Kubernetes 部署 | 单进程部署，简单 Docker 即可 |

### 7.3 关键决策与理由

| 决策 | 我的判断 |
|------|----------|
| 至少一次投递语义 | 重复可接受，丢失不可接受，实现简单 |
| 用 Redis 而非 RabbitMQ | 减少依赖，Redis 本身就需要用于持久化 |
| 指数退避重试 | 标准实践，避免雪崩外部系统 |
| 死信队列 | 人工干预的必要手段 |
| 不做请求转换 | 避免过度设计，业务系统知道格式 |
| 不做认证 | 内网环境，MVP 阶段简化 |

## 八、API 设计

### 8.1 接收通知接口

```
POST /api/v1/notify
Content-Type: application/json

Request:
{
  "url": "https://external-system.com/api/webhook",
  "headers": {
    "Authorization": "Bearer xxx",
    "Content-Type": "application/json"
  },
  "body": {
    "user_id": "123",
    "event": "register"
  }
}

Response:
{
  "task_id": "uuid-string",
  "status": "pending",
  "message": "Task queued successfully"
}
```

### 8.2 查询任务状态

```
GET /api/v1/notify/{task_id}

Response:
{
  "task_id": "uuid-string",
  "url": "https://external-system.com/api/webhook",
  "status": "success",
  "retry_count": 0,
  "created_at": "2025-01-06T10:00:00Z",
  "updated_at": "2025-01-06T10:00:01Z"
}
```

### 8.3 查询死信队列

```
GET /api/v1/dead-letters?limit=10&offset=0

Response:
{
  "total": 5,
  "items": [...]
}
```

### 8.4 重新投递死信任务

```
POST /api/v1/dead-letters/{task_id}/retry

Response:
{
  "message": "Task requeued successfully"
}
```

## 九、代码结构

```
api-notification-system/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI 应用入口
│   ├── api/
│   │   ├── __init__.py
│   │   └── notify.py        # 通知 API 路由
│   ├── models/
│   │   ├── __init__.py
│   │   └── task.py          # 任务数据模型
│   ├── services/
│   │   ├── __init__.py
│   │   ├── queue.py         # 队列服务
│   │   └── worker.py        # Worker 消费者
│   ├── config.py            # 配置管理
│   └── utils.py             # 工具函数
├── requirements.txt
├── README.md
├── AI_USAGE.md              # 本文件
└── docker-compose.yml       # Redis + 应用
```

## 十、部署与运维

### 10.1 本地开发

```bash
# 启动 Redis
docker-compose up -d redis

# 启动 API 服务
uvicorn app.main:app --reload --port 8000

# 启动 Worker
celery -A app.services.worker worker --loglevel=info
```

### 10.2 生产部署

```bash
# Docker 部署
docker-compose up -d

# 或手动部署
# 1. 启动 Redis
# 2. 启动 API 服务（gunicorn + uvicorn）
# 3. 启动 Worker（多进程）
```

### 10.3 监控指标

- 任务队列长度
- 任务处理速率
- 任务成功率
- 重试次数分布
- 死信队列积压量

## 十一、风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Redis 故障 | 任务丢失 | Redis AOF 持久化 + 主从复制 |
| 外部系统雪崩 | Worker 阻塞 | 限流 + 超时 + 死信队列 |
| 重复通知 | 业务异常 | 要求外部系统幂等 |
| 内存溢出 | 系统崩溃 | 限制并发数 + 监控 |

## 十二、总结

本设计遵循以下原则：
1. **简单优先**：MVP 阶段用最简单方案满足核心需求
2. **可靠为重**：至少一次投递 + 失败重试 + 死信队列
3. **边界清晰**：明确做什么、不做什么
4. **可演进**：为未来扩展留有空间
